{
  "name": "qa-core-heal",
  "version": "0.3.0-wrappers.0",
  "description": "Deterministic selector healing for Playwright specs. Fork with pageObjects.wrappers support (this.$).",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=22.6"
  },
  "bin": {
    "qa-core-heal": "dist/cli.js"
  },
  "files": [
    "dist",
    "skill",
    "README.md",
    "CHANGELOG.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "heal": "npm run build && node dist/cli.js",
    "demo": "npm run build && node example-project/scripts/demo.mjs",
    "evals": "npm run build && node evals/run-evals.mjs",
    "test": "npm run build && node --test tests/*.test.mjs",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "npm run build",
    "test:wrappers": "npm run build && node --test tests/wrapper.test.mjs"
  },
  "dependencies": {
    "@playwright/test": "1.60.0",
    "playwright": "1.60.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "dotenv": "^17.4.2",
    "typescript": "^5.6.0"
  }
}
